"use strict";
const logDetails = (uid, item) => {
    console.log(`${item} has uid of ${uid}`);
};
const radnom = (user) => {
    console.log(`${user.name} says hello`);
};
const radnom2 = (user) => {
    console.log(`${user.name} says hello`);
};
